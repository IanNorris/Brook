/* errno.h shim — lwext4 provides its own when CONFIG_HAVE_OWN_ERRNO=1.
 * This stub satisfies any #include <errno.h> that slips through. */
#pragma once

/* lwext4's ext4_errno.h defines all the errno values we need. */
