pcm.null {
    type null
}

pcm.brook_dsp_raw {
    type file
    slave.pcm "null"
    file "/dev/dsp"
    format "raw"
}

pcm.!default {
    type plug
    slave {
        pcm "brook_dsp_raw"
        format S16_LE
        rate 44100
        channels 2
    }
}

ctl.!default {
    type empty
}
